TELEGRAM_BOT_TOKEN=
OPENAI_API_KEY=
SUPABASE_URL=
# Server-only. Bypasses RLS — never ship to browsers or client code.
SUPABASE_SERVICE_ROLE_KEY=
# Deprecated / optional (Railway rollback only). Prefer SUPABASE_SERVICE_ROLE_KEY.
SUPABASE_ANON_KEY=
# When true/1/yes/on: hard-set moderation hits block turns. Default off = shadow-only.
MODERATION_ENFORCE=
